# TODO
- update styling of markdown posts to be more expressive for features like code blocks and quotes
- add rest of passport stamps, hikes, and places to travel page

## Code quality
- extract `cookbook/index.astro` category filter script to `src/scripts/cookbook-filter.js`
- check `wedding.astro` for inline JS (collapsible sections) and extract if present
- migrate `travel.yml`, `trails.yml`, `coffee.yml` from raw YAML imports to Astro Content Collections with schema validation